Before making any changes, run `git branch --show-current`. If the current branch is `main`, create and check out a new feature branch named `update/hero-YYYYMMDD` (use today's date) and tell the user which branch was created. If already on a feature branch, proceed without creating a new one.

Update the hero section in index.html. The hero is the large banner at the top of the page (section id="home") and contains a headline (h1), a subheadline paragraph, and two call-to-action buttons.

Read the current hero content from index.html and show it to the user. Then ask what they'd like to change:
- The main headline (currently "Florida's Trusted Wholesale Sod Supplier")
- The subheadline paragraph
- The button text or links

Make only the changes the user asks for. Keep the HTML structure intact.
