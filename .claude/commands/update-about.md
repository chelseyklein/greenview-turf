Before making any changes, run `git branch --show-current`. If the current branch is `main`, create and check out a new feature branch named `update/about-YYYYMMDD` (use today's date) and tell the user which branch was created. If already on a feature branch, proceed without creating a new one.

Update the About Us section in index.html. The about section is `#about` and contains two paragraphs of text, a heading, and three stats (years experience, days a week, statewide supply).

Read the current about section content from index.html and show it to the user. Then ask them what they'd like to change — it could be:
- The heading ("Rooted in Experience")
- The body paragraphs (the text about Mitchell Sims and Steve Wilson)
- The stats (the numbers and labels)
- The section label ("About Us")

Make only the changes the user asks for. Keep the HTML structure intact.
